# Starship Configuration
# Optimized for git, Python, and C# development
# Powerline-style with colorful segments
# Theme: Gruvbox

# Get editor completions based on the config schema
"$schema" = 'https://starship.rs/config-schema.json'

# Timeout for starship to scan files (milliseconds)
scan_timeout = 30

# Add a new line before the prompt
add_newline = true

# Format: Powerline-style colorful top line + separate command line

# UsedMarks for copypaste:   
format = """
[ ](fg:#458588 )\
 $os\
$directory\
[](fg:#458588 bg:#98971a)\
$git_branch\
$git_status\
[](fg:#98971a bg:#689d6a)\
$python\
$dotnet\
$nodejs\
$rust\
$golang\
[](fg:#689d6a bg:#d79921)\
$time\
[](fg:#d79921)\
$line_break\
$character"""

palette = 'gruvbox_dark'

[palettes.gruvbox_dark]
color_fg0 = '#fbf1c7'
color_fg1 = '#282828'
color_bg1 = '#3c3836'
color_bg3 = '#665c54'
color_blue = '#458588'
color_aqua = '#689d6a'
color_green = '#98971a'
color_orange = '#d65d0e'
color_purple = '#b16286'
color_red = '#cc241d'
color_yellow = '#d79921'

# Prompt character (on separate line for input)
[character]
success_symbol = "[❯](bold green)"
error_symbol = "[❯](bold red)"
vimcmd_symbol = "[❮](bold green)"

[os]
disabled = false
style = "bg:color_blue fg:color_fg1"

[os.symbols]
Windows = "󰍲 "
Ubuntu = "󰕈 "
SUSE = ""
Raspbian = "󰐿"
Mint = "󰣭"
Macos = "󰀵"
Manjaro = ""
Linux = "󰌽"
Gentoo = "󰣨"
Fedora = "󰣛"
Alpine = ""
Amazon = ""
Android = ""
AOSC = ""
Arch = "󰣇"
Artix = "󰣇"
EndeavourOS = ""
CentOS = ""
Debian = "󰣚"
Redhat = "󱄛"
RedHatEnterprise = "󱄛"
Pop = ""

# Username - powerline style
[username]
show_always = true
style_user = "bg:color_purple fg:color_fg1"
style_root = "bg:color_purple fg:color_fg1"
format = '[$user ]($style)'
disabled = false

# Directory - powerline style
[directory]
style = "bg:color_blue fg:color_fg1"
format = "[$path]($style)[$read_only]($read_only_style)"
truncation_length = 7
truncation_symbol = "…/"
truncate_to_repo = false
read_only = ""
read_only_style = "bg:color_blue fg:color_fg1"
repo_root_style = "bold bg:color_blue fg:#000000"
repo_root_format = "[$before_root_path]($before_repo_root_style)[$repo_root]($repo_root_style)[$path]($style)[$read_only]($read_only_style)"

# Git Branch - powerline style
[git_branch]
format = "[ $symbol$branch ]($style)"
style = "bg:#98971a fg:color_fg1"

# Git Status - powerline style with detailed info
[git_status]
format = '([$all_status$ahead_behind ]($style))'
style = "bg:#98971a fg:color_fg1"
conflicted = "⚔️${count} "
ahead = "⇡${count} "
behind = "⇣${count} "
diverged = "⇕${ahead_count}⇣${behind_count} "
untracked = "?${count} "
stashed = "" #📦${count}
modified = "!${count} "
staged = "+${count} "
renamed = "»${count} "
deleted = "✘${count} "

# Python - powerline style
[python]
symbol = "🐍 "
format = '[ $symbol($version )(\($virtualenv\) )]($style)'
style = "bg:#689d6a fg:color_fg1"
pyenv_version_name = false
python_binary = ["python3", "python"]
detect_extensions = ["py"]
detect_files = ["requirements.txt", ".python-version", "pyproject.toml", "Pipfile", "setup.py", "tox.ini", "setup.cfg"]
detect_folders = []

# .NET / C# - powerline style
[dotnet]
symbol = "🎯 "
format = "[ $symbol($version )]($style)"
style = "bg:#689d6a fg:color_fg1"
heuristic = true
detect_extensions = ["csproj", "fsproj", "xproj", "sln"]
detect_files = ["global.json", "project.json", "Directory.Build.props", "Directory.Build.targets", "Packages.props"]
detect_folders = []

# Node.js - powerline style
[nodejs]
symbol = " "
format = "[ $symbol($version )]($style)"
style = "bg:#689d6a fg:color_fg1"
detect_extensions = ["js", "mjs", "cjs", "ts", "mts", "cts"]
detect_files = ["package.json", ".node-version", ".nvmrc"]
detect_folders = ["node_modules"]

# Rust - powerline style
[rust]
symbol = "🦀 "
format = "[ $symbol($version )]($style)"
style = "bg:#689d6a fg:color_fg1"
detect_extensions = ["rs"]
detect_files = ["Cargo.toml"]
detect_folders = []

# Go - powerline style
[golang]
symbol = " "
format = "[ $symbol($version )]($style)"
style = "bg:#689d6a fg:color_fg1"
detect_extensions = ["go"]
detect_files = ["go.mod", "go.sum", "go.work", "glide.yaml", "Gopkg.yml", "Gopkg.lock"]
detect_folders = ["Godeps"]

# Command duration
[cmd_duration]
min_time = 500
format = "took [$duration]($style) "
style = "bold yellow"

# Battery (for laptops)
[battery]
full_symbol = "🔋"
charging_symbol = "⚡"
discharging_symbol = "💀"
unknown_symbol = "❓"
empty_symbol = "🪫"

[[battery.display]]
threshold = 20
style = "bold red"

[[battery.display]]
threshold = 50
style = "bold yellow"

[[battery.display]]
threshold = 100
style = "bold green"

# Time - enabled and styled for powerline
[time]
disabled = false
format = '[ 🕙 $time ]($style)'
time_format = "%R"
style = "bg:#d79921 fg:color_fg1"

# Disable modules you don't need
[aws]
disabled = true

[gcloud]
disabled = true

[kubernetes]
disabled = true

[docker_context]
disabled = true
