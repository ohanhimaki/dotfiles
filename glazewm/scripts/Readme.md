
# Publishing

Use `.\PublishScript.ps1` instead of the old C# helper when you want to publish the app locally without creating a temporary helper executable first.

Examples:

`.\PublishScript.ps1`

`.\PublishScript.ps1 -CopyToBin`

`.\PublishScript.ps1 -NoCopy`
